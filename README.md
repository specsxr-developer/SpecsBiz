
# SpecsBiz | Smart Business Manager

এটি একটি প্রফেশনাল বিজনেস ম্যানেজমেন্ট ওয়েব অ্যাপ্লিকেশন। নিচে এটি অনলাইনে পাবলিশ করা এবং ভবিষ্যতে এপিকে (APK) তৈরি করার পূর্ণাঙ্গ গাইড দেওয়া হলো।

## 🌐 অনলাইনে লাইভ করার নিয়ম (Vercel Deployment)

আপনার অ্যাপটি আজীবন ফ্রিতে চালানোর জন্য নিচের ধাপগুলো অনুসরণ করুন:

### ধাপ ১: GitHub-এ কোড আপলোড
১. আপনার পিসিতে এই প্রোজেক্টের ফোল্ডারে টার্মিনাল ওপেন করুন।
২. নিচের কমান্ডগুলো সিরিয়ালি দিন:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin [আপনার-github-রিপোজিটরি-লিঙ্ক]
   git push -u origin main
   ```

### ধাপ ২: Vercel-এ সেটআপ
১. [Vercel.com](https://vercel.com/)-এ গিয়ে GitHub দিয়ে লগইন করুন।
২. **Add New Project**-এ ক্লিক করে আপনার প্রোজেক্টটি **Import** করুন।
৩. **Environment Variables** সেকশনে গিয়ে `GEMINI_API_KEY` যোগ করুন।
৪. **Deploy** বাটনে ক্লিক করুন।

---

## 📱 ভবিষ্যতে এপিকে (APK) বানানোর নিয়ম

আপনি যদি ভবিষ্যতে এটিকে অ্যান্ড্রয়েড অ্যাপ হিসেবে ফোনে ইনস্টল করতে চান, তবে নিচের ধাপগুলো অনুসরণ করবেন:

১. **Android Studio** আপনার পিসিতে ইনস্টল থাকতে হবে।
২. টার্মিনালে এই কমান্ডটি দিন:
   ```bash
   npm run mobile:build
   ```
৩. এরপর অ্যান্ড্রয়েড স্টুডিও ওপেন করতে নিচের কমান্ডটি দিন:
   ```bash
   npm run mobile:open
   ```
৪. অ্যান্ড্রয়েড স্টুডিও থেকে **Build > Build Bundle(s) / APK > Build APK(s)** সিলেক্ট করলে আপনার ফোনে চালানোর জন্য এপিকে ফাইল তৈরি হয়ে যাবে।

*দ্রষ্টব্য: এপিকে মোডে এআই (AI) চালানোর জন্য আপনাকে পরবর্তীতে ক্লায়েন্ট-সাইড কনফিগারেশন আপডেট করতে হতে পারে।*

---

## 🔄 ভবিষ্যতে অ্যাপ আপডেট করার নিয়ম

আপনি যদি পরে কোডে কোনো পরিবর্তন করেন, তবে লাইভ সাইট আপডেট করা খুব সহজ:

১. টার্মিনালে নিচের ৩টি কমান্ড দিন:
   ```bash
   git add .
   git commit -m "নতুন আপডেট যোগ করা হয়েছে"
   git push origin main
   ```
২. **ব্যাস!** Vercel অটোমেটিক আপনার পরিবর্তনগুলো লাইভ সাইটে আপডেট করে নেবে।

---

## 🛠 প্রযুক্তি (Tech Stack)
- **Framework:** Next.js 15
- **Database:** Firebase Firestore
- **Auth:** Firebase Authentication
- **AI:** Google Genkit (Gemini 2.5 Flash)
- **UI:** Tailwind CSS + ShadCN

Developed by **SpecsXR**.
