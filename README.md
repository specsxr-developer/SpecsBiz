
# SpecsBiz | Smart Business Manager

এটি একটি প্রফেশনাল বিজনেস ম্যানেজমেন্ট ওয়েব অ্যাপ্লিকেশন। নিচে এটি অনলাইনে পাবলিশ করা এবং ভবিষ্যতে আপডেট করার পূর্ণাঙ্গ গাইড দেওয়া হলো।

## 🌐 অনলাইনে লাইভ করার নিয়ম (Vercel Deployment)

আপনার অ্যাপটি আজীবন ফ্রিতে চালানোর জন্য নিচের ধাপগুলো অনুসরণ করুন:

### ধাপ ১: GitHub-এ কোড আপলোড
১. আপনার পিসিতে এই প্রোজেক্টের ফোল্ডারে টার্মিনাল ওপেন করুন।
২. নিচের কমান্ডগুলো সিরিয়ালি দিন:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin [আপনার-github-রিপোজিটরি-লিঙ্ক]
   git push -u origin main
   ```

### ধাপ ২: Vercel-এ সেটআপ
১. [Vercel.com](https://vercel.com/)-এ গিয়ে আপনার GitHub অ্যাকাউন্ট দিয়ে লগইন করুন।
২. **Add New Project**-এ ক্লিক করে আপনার প্রোজেক্টটি **Import** করুন।
৩. **Environment Variables** সেকশনে গিয়ে আপনার `.env` ফাইলের সব তথ্য যোগ করুন (যেমন: `GEMINI_API_KEY`)।
৪. **Deploy** বাটনে ক্লিক করুন। ২ মিনিটের মধ্যে আপনার অ্যাপ লাইভ হয়ে যাবে।

---

## 🔄 ভবিষ্যতে অ্যাপ আপডেট করার নিয়ম

আপনি যদি পরে কোডে কোনো পরিবর্তন করেন, তবে লাইভ সাইট আপডেট করা খুব সহজ:

১. আপনার পিসিতে কোড এডিট শেষ করুন।
২. টার্মিনালে নিচের ৩টি কমান্ড দিন:
   ```bash
   git add .
   git commit -m "নতুন আপডেট যোগ করা হয়েছে"
   git push origin main
   ```
৩. **ব্যাস!** Vercel অটোমেটিক আপনার পরিবর্তনগুলো লাইভ সাইটে আপডেট করে নেবে। আপনাকে আর কিছু করতে হবে না।

---

## 🛠 প্রযুক্তি (Tech Stack)
- **Framework:** Next.js 15
- **Database:** Firebase Firestore
- **Auth:** Firebase Authentication
- **AI:** Google Genkit (Gemini 2.5 Flash)
- **UI:** Tailwind CSS + ShadCN

Developed by **SpecsXR**.
